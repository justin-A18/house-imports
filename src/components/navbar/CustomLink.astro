---
interface Props {
	title: string;
	to: string;
	className?: string;
}

const { title, to, className } = Astro.props;
---

<a
	href={to}
	class={`bg-amber-300 px-4 py-2 rounded-md font-bold ${className}`}>
	{title}
</a>
